name: hse_frontend
description: HSEBOOK Frontend - Health Safety Environment Book
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  
  # Local Storage & Caching
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Connectivity
  connectivity_plus: ^6.0.5
  
  # Social Auth (placeholder - implement with actual social auth packages)
  # google_sign_in: ^6.2.1
  # flutter_facebook_auth: ^6.1.1
  
  # Utilities
  shared_preferences: ^2.3.2
  intl: 0.19.0
  image_picker: ^1.0.7
  flutter_secure_storage: ^9.0.0
  
  # Charts & Graphs
  fl_chart: ^0.66.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
